
import React from 'react';
import { PartyPopper as PartyPopperIcon } from 'lucide-react';

const PartyPopper = (props: React.ComponentProps<typeof PartyPopperIcon>) => {
  return <PartyPopperIcon {...props} />;
};

export default PartyPopper;
